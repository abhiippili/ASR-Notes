import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "rgba(0,0,0,0)"
    },
    secondary: {
      main: "#000"
    },
    info: {
      main: "#fff"
    },
    background: {
      default: "#ffd700"
    }
  }
});
